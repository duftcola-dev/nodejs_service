
1 ) Connecting to mongodb : 

    uri = //"mongodb://user:user_password@localhost:27017/ 

    where localhost:27017 is the host and port
    and user:user_password are the registered user and password. 

    Initially the database can be acceced with  root:root

    let client = MongoClient.connect(uri)
    result = await client.db(database).collection(collection).insertOne(query);
        console.log(JSON.stringify(result)); ** show results as string
    return result;


    ** Sometimes the databse returns data in the form of an array :

    result = await client.db(database).collection(collection).find({}).toArray();